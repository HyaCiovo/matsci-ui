import type { SearchUIContainerProps } from '../types';
import { SearchUIContainer } from './SearchUIContainer';

export const MatscholarSearchUIContainer = (props: SearchUIContainerProps) => {
  return <SearchUIContainer {...props} />;
};
